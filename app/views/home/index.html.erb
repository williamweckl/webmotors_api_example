<%= form_tag models_path, method: :get do %>
    <p>
      <%= label_tag 'Escolha a Fabricante:' %>

      <%= select_tag :brand_id, options_from_collection_for_select(@brands, "id", "name") %>
    </p>

    <%= submit_tag "Buscar modelos" %>
<% end %>